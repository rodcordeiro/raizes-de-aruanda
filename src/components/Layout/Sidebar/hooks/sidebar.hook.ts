// import { connection } from '@/lib/db';
// import React from 'react';
export function SidebarHook() {
  // const db = React.useMemo(() => connection(), []);
  //   React.useEffect(() => {
  //     db.select('*')
  //       .from('tb_pontos')
  //       .then(rows => console.log(rows));
  //   }, [db]);
}
